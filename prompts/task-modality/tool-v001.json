{
  "name": "report_modality",
  "description": "Report what the task demands of the person answering it.",
  "parameters": {
    "type": "object",
    "properties": {
      "verdict": {
        "type": "string",
        "enum": ["do", "explain", "unsure"],
        "description": "do when the task asks the person to do something; explain when it asks them to explain; unsure when you cannot tell"
      },
      "reason": {
        "type": "string",
        "description": "The reason for the verdict, in one sentence."
      }
    },
    "required": ["verdict", "reason"]
  }
}
