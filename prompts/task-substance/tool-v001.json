{
  "name": "report_substance",
  "description": "Report whether answering the task takes knowledge of the subject.",
  "parameters": {
    "type": "object",
    "properties": {
      "verdict": {
        "type": "string",
        "enum": ["substantive", "trivial", "unsure"],
        "description": "substantive when answering takes knowledge of the subject; trivial when someone who never studied it could answer just as well; unsure when you cannot tell"
      }
    },
    "required": ["verdict"]
  }
}
