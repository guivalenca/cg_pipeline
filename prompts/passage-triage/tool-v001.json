{
  "name": "report_verdict",
  "description": "Report the verdict on the passage.",
  "parameters": {
    "type": "object",
    "properties": {
      "verdict": {
        "type": "string",
        "enum": ["filler", "not_filler", "unsure"],
        "description": "unsure when you cannot tell."
      }
    },
    "required": ["verdict"]
  }
}
