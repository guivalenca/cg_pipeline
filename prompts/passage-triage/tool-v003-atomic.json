{
  "name": "report_passage_decision",
  "description": "Report how the atomic passage should be handled.",
  "parameters": {
    "type": "object",
    "properties": {
      "verdict": {
        "type": "string",
        "enum": ["keep", "drop", "unknown"]
      }
    },
    "required": ["verdict"],
    "additionalProperties": false
  }
}
