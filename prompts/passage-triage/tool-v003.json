{
  "name": "report_passage_decision",
  "description": "Report how the passage should be handled.",
  "parameters": {
    "type": "object",
    "properties": {
      "verdict": {
        "type": "string",
        "enum": ["keep", "drop", "refine", "unknown"]
      }
    },
    "required": ["verdict"],
    "additionalProperties": false
  }
}
