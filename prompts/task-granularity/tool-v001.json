{
  "name": "report_granularity",
  "description": "Report whether the task asks one question or several, splitting it when several.",
  "parameters": {
    "type": "object",
    "properties": {
      "verdict": {
        "type": "string",
        "enum": ["single", "composite", "unsure"],
        "description": "single when the task asks one question; composite when it packs several; unsure when you cannot tell"
      },
      "parts": {
        "type": "array",
        "description": "The split tasks, only when the verdict is composite",
        "items": {
          "type": "object",
          "properties": {
            "task": {"type": "string"},
            "answer": {"type": "string"}
          },
          "required": ["task", "answer"]
        }
      }
    },
    "required": ["verdict"]
  }
}
