{
  "name": "report_knowledge",
  "description": "Report which kind of knowledge answering the task rests on.",
  "parameters": {
    "type": "object",
    "properties": {
      "verdict": {
        "type": "string",
        "enum": ["concept", "procedure", "unsure"],
        "description": "unsure when you cannot tell whether the knowledge is concept or procedure"
      },
      "reason": {
        "type": "string",
        "description": "The reason for the verdict, in one sentence."
      }
    },
    "required": ["verdict", "reason"]
  }
}
