Use the report_line_removals tool to identify complete lines that can be removed from this passage without losing anything the source teaches.

The passage is shown with local line numbers from 1 to N. Return only ranges of those line numbers. The application will delete those exact lines; you must not rewrite, summarize, reorder, merge, or replace any text.

Remove only lines that are unequivocally disposable. Preserve educational content, necessary context, headings that organize retained content, and useful image descriptions. When a Markdown structure is atomic (for example a table, fenced code block, blockquote, list item, or image), remove the complete structure or none of it. At least one line must be removed; otherwise this refine decision cannot make progress.

<numbered_passage>
{{passage}}
</numbered_passage>
