{
  "name": "report_element_removals",
  "description": "Report passage elements to remove.",
  "parameters": {
    "type": "object",
    "properties": {
      "drop_elements": {
        "type": "array",
        "items": {"type": "integer", "minimum": 1}
      }
    },
    "required": ["drop_elements"],
    "additionalProperties": false
  }
}
