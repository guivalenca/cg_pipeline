{
  "name": "report_verdict",
  "description": "Report the verdict on the task.",
  "parameters": {
    "type": "object",
    "properties": {
      "verdict": {
        "type": "string",
        "enum": ["supported", "unsupported", "unsure"],
        "description": "unsure when you cannot tell."
      }
    },
    "required": ["verdict"]
  }
}
