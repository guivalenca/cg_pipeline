You will read a source and inspect the images referenced by it.

Use the report_source_images tool to decide which images should remain in the source. Retain an image only when it adds teachable content that is not adequately represented by the source text.

For each retained image, report its visible text and describe the visual information that is not already captured by that text. Report limitations only when part of the image cannot be analyzed reliably.

For images that should not remain, return null for ocr, description, and limitations.

<source>
{{body}}
</source>
