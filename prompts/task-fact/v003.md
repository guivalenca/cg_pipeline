You will read one task written for learners, with the answer it expects.

Some tasks rest on a pure connection — the answer is only a name, a
label, a chosen value. Two marks tell such a connection: it holds for
one fixed thing, never stretching to new cases; and no reason stands
behind it — it simply was chosen so.

Judge the knowledge behind the answer, not the words of the question.

Use the report_fact tool: is the knowledge behind this task such a
connection?

<task>{{task}}</task>

<answer>{{answer}}</answer>
