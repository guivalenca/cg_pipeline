{
  "name": "report_fact",
  "description": "Report whether the knowledge behind the task is such a connection.",
  "parameters": {
    "type": "object",
    "properties": {
      "verdict": {
        "type": "string",
        "enum": ["fact", "not_fact"],
        "description": "Whether the knowledge is a simple connection with no cause behind it."
      },
      "reason": {
        "type": "string",
        "description": "The reason for the verdict, in one sentence."
      }
    },
    "required": ["verdict", "reason"]
  }
}
