{
  "name": "report_cuts",
  "description": "Report where new passages begin in the document.",
  "parameters": {
    "type": "object",
    "properties": {
      "cuts": {
        "type": "array",
        "items": { "type": "integer" },
        "description": "Block numbers where a new passage begins, in ascending order. The first passage begins at the first block; do not include it."
      }
    },
    "required": ["cuts"]
  }
}
