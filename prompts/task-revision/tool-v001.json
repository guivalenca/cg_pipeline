{
  "name": "report_revision",
  "description": "Report whether the task stands on its own.",
  "parameters": {
    "type": "object",
    "properties": {
      "verdict": {
        "type": "string",
        "enum": ["stands", "rewritten", "unfixable"],
        "description": "unfixable when rewriting would mean guessing what the task points at."
      },
      "task": {
        "type": "string",
        "description": "The rewritten task, only when the verdict is rewritten."
      }
    },
    "required": ["verdict"]
  }
}
