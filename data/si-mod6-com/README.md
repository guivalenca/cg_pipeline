# SI Module 6 Computacao source fixture

This fixture mirrors every SI Module 6 Computacao Source Body preserved in the
CG pipeline historical archive.

- 69 Self-study records are present in the source ledger.
- 67 have usable extracted Markdown Source Bodies under `source-bodies/`.
- 2 are unavailable and remain represented in
  `reference/unavailable-sources.json` and `reference/source-inventory.json`.
- The original workbook is preserved as `reference/si_mod6.xlsx`.
- The archived build and runtime graphs are preserved under `reference/` for
  comparison with new Concept Universe experiments.

The Perceptron materials previously presented as a separate slice are part of
the complete source collection: source IDs 45, 46, 49, and 50.

Treat the source files as immutable test inputs. Write derived Fragments and
later artifacts to separate run directories rather than modifying these files.

Copied from `/Users/guilhermevalenca/Desktop/cg_pipeline/archive/si-mod6-com`.
